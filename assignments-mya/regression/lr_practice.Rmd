---
title: "Построение модели линейной регрессии"
author: "Студент Школы логистики"
date: '8 февраля 2018 г '
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE, warning = FALSE)
options(width = 100) # ширина текстового вывода
options(digits = 3) # число знаков после запятой в выводе 
```


#Введение

В файле `checkout.xls` содержатся данные о времени обслуживания на кассе 18 покупателей:  
- `customer` - номер покупателя;  
- `time` - время обслуживания;  
- `amount` - сумма покупки;
- `items` - количество товаров в чеке.


```{r Загрузка пакетов, message = F} 
library(readxl) # считывание данных из файлов Excel
library(tidyverse) # манипулирование данными 
library(GGally) # построение матрицы диаграмм рассеяния 
library(ggfortify) # визуализация диагностических графиков
library(modelr) # вспомогательные функции для работы с моделями
library(broom) # преобразование результатов моделирования в табличный вид
```


#Задания

1. Загрузите данные.

```{r Загрузка данных}

```

2. Постройте корреляционную матрицу времени обслуживания, суммы покупки и количества товаров.

```{r Построение корреляционной матрицы}

```

3. Постройте две модели простой линейной регрессии для зависимостей времени обслуживания от суммы покупки и от количества товаров. Запишите для каждой модели формулу для прогнозирования времени обслуживания (на основе вывода lm).
Проверьте значимость модели с помощью F-критерия.
Проверьте значимость коэффициентов модели с помощью t-критериев.

```{r Построение моделей линейной регрессии}

```

4. Постройте доверительные интервалы для среднего значения и отдельных наблюдений. Какая из двух моделей наиболее точная?

```{r Построение доверительных интервалов}

```

