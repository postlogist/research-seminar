---
title: "Соревнование по прогнозированию объемов генерации электроэнергии из возобновляемых источников"
author: "Студент школы логистики, не умеющий править тег `author`"
output: 
  html_document: 
    toc: yes
---


```{r Настройка, include=TRUE}
knitr::opts_chunk$set(dev = 'png', warning = FALSE) 
# выбор формата для графиков и отключение жалоб на русские шрифты на графиках
options(digits = 4) # Количество значащих цифр при выводе
suppressMessages(library(readxl))
suppressMessages(library(forecast))
suppressMessages(library(ggfortify))
```


## Постановка задачи

В файле `wind0.xlsx` содержатся исходные данные об объемах генерации электроэнергии из возобновляемых источников (ветер). Код для загрузки данных дан в следующем блоке.

```{r Код для загрузки данных}
df <- read_excel('wind0.xlsx')
GWh <- ts(df$GWh, frequency = 12)
autoplot(GWh)+ labs(title = 'Генерация электроэнергии в США', y = 'ГВт-ч', x = NULL)
```



Ваша задача - проанализировать структуру временного ряда, подобрать наиболее подходящую модель прогнозирования и сделать прогноз на 12 месяцев вперед от имеющихся данных (горизонт прогнозирования - 12 месяцев).

# Условия соревнования

- Ответы принимаются с 9 декабря по 18 декабря включительно.
- Можно участвовать единолично или в команде из не более чем 3 участников.
- Все не дисквалифицированные участники соревнования получают одну активность.
- Победители соревнования получают еще одну активность дополнительно.
- В качестве победителей будут выбраны две работы - с наиболее точным прогнозом на контрольном периоде (по критерию MAPE) и с наиболее качественным отчетом по решению. Возможна ситуация, в которой несколько работ будут иметь один показатель ошибки. В этом случае победителем будет признана первая полученная работа.
- Дисквалификация участников возможна по следующим причинам: плагиат, отсутствие исследования структуры ряда и обоснования выбора модели, неспособность участников предоставить результаты в требуемом формате.

# Здесь ваше решение

...

# Как сдавать

Результатом вашей работы являются два файла: 

  - отчет в формате HTML, полученный с помощью кнопки Knit из данного файла. Отредактируйте тег `author:` в начале этого документа, указав свою фамилию (фамилии) и группу;
  
  - файл output.RDS, содержащий ваш прогноз на 12 будущих периодов и больше ничего другого.
  
  Прогнозы будут обрабатываться автоматическим скриптом, поэтому несоответствие требуемому формату означает невозможность учесть ваш результат и дисквалификацию работы.
  
Ниже дан пример кода, который строит прогноз наивным методом и сохраняет его в нужном формате.

```{r Сохранение результатов}
my_model <- naive(GWh, h = 12)
my_forecast <- my_model$mean
# Вывод на печать
my_forecast
# Вывод в файл
saveRDS(my_forecast, 'output.RDS')

```

